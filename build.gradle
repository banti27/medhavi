plugins {
    id 'java'
    id 'application'
}

application {
    // Interactive CLI entrypoint
    mainClass = 'com.medhavi.qa.TextQAApplication'
}

// Needed for interactive CLI input when running via Gradle
tasks.named('run') {
    standardInput = System.in
}

tasks.named('run') {
    // This line is crucial for interactive applications; it connects the system input to the running task
    standardInput = System.in
}

group = 'com.medhavi'
version = '1.0-SNAPSHOT'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

repositories {
    mavenCentral()
}

ext {
    dl4jVersion = '1.0.0-M2.1'
    nd4jBackend = 'nd4j-native-platform'
    commonsIoVersion = '2.15.1'
    slf4jVersion = '2.0.9'
    junitVersion = '5.10.1'
    apachePdfBoxVersion = '2.0.24'
    jlineVersion = '3.25.1'
    jacksonVersion = '2.17.2'
}

dependencies {
    // DeepLearning4j Core
    implementation "org.deeplearning4j:deeplearning4j-core:${dl4jVersion}"
    
    // ND4J Native Backend (CPU)
    implementation "org.nd4j:${nd4jBackend}:${dl4jVersion}"
    implementation "org.nd4j:nd4j-native:${dl4jVersion}:macosx-arm64"
    implementation "org.bytedeco:openblas:0.3.21-1.5.8:macosx-arm64"
    
    // DeepLearning4j NLP
    implementation "org.deeplearning4j:deeplearning4j-nlp:${dl4jVersion}"
    
    // Apache Commons IO for file operations
    implementation "commons-io:commons-io:${commonsIoVersion}"

    // Apache PDFBox for PDF document handling
    implementation "org.apache.pdfbox:pdfbox:${apachePdfBoxVersion}"

    // SLF4J Logging
    implementation "org.slf4j:slf4j-api:${slf4jVersion}"
    implementation "org.slf4j:slf4j-simple:${slf4jVersion}"

    // JLine 3 (interactive console: history, line editing, better terminal handling)
    implementation "org.jline:jline:${jlineVersion}"

    // JSON (for Ollama HTTP API requests/responses)
    implementation "com.fasterxml.jackson.core:jackson-databind:${jacksonVersion}"

    // JUnit for testing
    testImplementation "org.junit.jupiter:junit-jupiter:${junitVersion}"
    testRuntimeOnly "org.junit.platform:junit-platform-launcher"
}

tasks.named('test') {
    useJUnitPlatform()
}

// Configure the JAR with manifest
jar {
    manifest {
        attributes 'Main-Class': 'com.medhavi.qa.TextQAApplication'
    }
}

// ShadowJar config removed: shadow plugin is not applied in this build.

// Custom task to run the application with a file path argument
task runApp(type: JavaExec) {
    group = 'application'
    description = 'Run the Text QA Application with a file path'
    classpath = sourceSets.main.runtimeClasspath
    mainClass = 'com.medhavi.qa.TextQAApplicationRunner'
    standardInput = System.in
    
    // Get file path from command line arguments
    if (project.hasProperty('args')) {
        args = project.args.split('\\s+')
    }
}

// Disable slow distribution tasks
startScripts.enabled = false
distZip.enabled = false
distTar.enabled = false

